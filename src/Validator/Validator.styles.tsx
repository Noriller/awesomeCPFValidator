import { StyleSheet } from 'react-native';

export const ValidatorStyles = StyleSheet.create({
  text: {
    fontSize: 40,
    fontWeight: 'bold',
    color: 'rgba(10, 146, 148, 0.5)',
  },
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
